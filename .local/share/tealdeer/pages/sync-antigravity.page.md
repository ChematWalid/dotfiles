# sync-antigravity

> High-performance Rust bidirectional synchronizer between Antigravity GUI and CLI.

- Run bidirectional conversation synchronization:
  `sync-antigravity`

- Check background systemd timer status:
  `systemctl --user status sync-antigravity.timer`
