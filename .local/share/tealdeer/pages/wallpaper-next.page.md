# wallpaper-next

> Pick and apply a new random wallpaper immediately and reset rotation timer.

- Switch to next random wallpaper immediately:
  `wallpaper-next.sh`

- Keyboard shortcut to switch wallpaper:
  `<Super + N>`
