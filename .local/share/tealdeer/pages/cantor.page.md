# cantor

> Adding additional application library path for Cantor plugins loading "/usr/lib/qt6/plugins"
> Managed locally in ~/.local/bin/cantor.

- Run default action:
  `cantor`

- Show help and usage details:
  `cantor --help`
