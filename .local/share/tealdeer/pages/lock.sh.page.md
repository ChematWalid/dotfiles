# lock.sh

> Aesthetic Gaussian-blurred Catppuccin Mocha lockscreen using i3lock-color.

- Lock the screen immediately:
  `lock.sh`

- Keyboard shortcut to lock screen:
  `<Win + L> or <Alt + Ctrl + L>`
