# update

> System maintenance shorthand: full Arch Linux upgrade (pacman + AUR via yay) followed by cache cleanup.

- Upgrade all system packages, AUR packages, and clean old cache files:
  `update`

- Clean package cache, orphaned packages, and vacuum journal logs:
  `cleanup`
