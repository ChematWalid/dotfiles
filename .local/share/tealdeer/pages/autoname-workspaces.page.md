# autoname-workspaces

> Dynamic i3 workspace icon namer daemon written in Rust.
> Automatically adds application Nerd Font icons to workspace numbers based on open windows.

- Run workspace auto-naming daemon in foreground:
  `autoname-workspaces`

- Check background user service status:
  `systemctl --user status i3-autoname.service`
