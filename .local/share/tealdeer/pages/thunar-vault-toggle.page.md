# thunar-vault-toggle

> Interactive gocryptfs encrypted vault lock/unlock helper for Thunar and CLI with Zenity.

- Toggle lock or unlock for the default Secret vault:
  `thunar-vault-toggle`

- Toggle lock or unlock for a specific target folder:
  `thunar-vault-toggle {{path/to/folder}}`
