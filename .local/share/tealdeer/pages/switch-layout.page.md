# switch-layout

> Toggles keyboard layout between Belgian (AZERTY) and Arabic with Dunst OSD notification.

- Switch keyboard layout between Belgian and Arabic:
  `switch-layout.sh`

- Keyboard shortcut to toggle layout:
  `<Win + Space>`
