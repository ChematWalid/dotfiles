# bandwhich

> Terminal bandwidth utilization monitor showing usage per process, connection, and IP in Rust.

- Run bandwhich with root privileges:
  `sudo bandwhich`

- Monitor a specific network interface:
  `sudo bandwhich -i <interface>`

- Show processes table only:
  `sudo bandwhich -p`

- Show connections table only:
  `sudo bandwhich -c`
