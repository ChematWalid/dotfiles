# apply-input-settings

> Re-applies X11 mouse acceleration, pointer sensitivity, and keyboard repeat rates.
> Automatically re-run after unlocking screen or reconnecting USB input devices.

- Apply input settings immediately:
  `apply-input-settings.sh`

- Apply without spawning background watcher daemon:
  `apply-input-settings.sh --no-spawn`
