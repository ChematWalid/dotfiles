% system, arch, pacman, packages

# Update all packages (pacman + AUR via yay)
update

# Full system upgrade with AUR
yay -Syu

# Install a package (AUR-aware)
yay -S <package>

# Remove package and unused dependencies
sudo pacman -Rns <package>

# Search for a package
yay -Ss <package>

# List explicitly installed packages
pacman -Qqe

# Clean pacman cache, orphans, and journal
cleanup

# Clean only pacman cache (keep last 2 versions)
sudo paccache -r

# Downgrade a package to a previous version
downgrade <package>

# Show package info
pacman -Qi <package>

# Show files owned by a package
pacman -Ql <package>

# Find which package owns a file
pacman -Qo <file>

# List orphaned packages
pacman -Qtdq

# Check for failed systemd services
systemctl --failed

# Enable and start a service
sudo systemctl enable --now <service>

# Restart a service
sudo systemctl restart <service>

# Show service logs (live)
journalctl -fu <service>

# Vacuum journal logs older than 3 days
sudo journalctl --vacuum-time=3d

# UFW firewall status
sudo ufw status verbose

# Allow a port through firewall
sudo ufw allow <port>

# Show CPU power info
cpupower frequency-info

# Set CPU governor
sudo cpupower frequency-set -g <governor>

% system, monitoring, resources

# All-in-one system overview (glances)
sys

# Interactive process monitor (btop)
btop

# GPU usage monitor (nvtop)
nvtop

# Disk usage visual, biggest at bottom (dust)
dust

# Disk usage interactive ncurses (gdu)
du

# Disk space overview, modern df (duf)
df

# Network bandwidth monitor per process
bandwhich

# Show smart disk health
sudo smartctl -a /dev/<disk>

# Show running processes (procs)
ps

% files, navigation, search

# Open Yazi async file manager
y

# Open LF terminal file manager
lf

# Open Superfile file manager
spf

# Interactive broot tree navigator
b

# Long list with icons, git status, hidden files
ll

# List files with icons, grouped dirs first
ls

# Tree view 2 levels deep
lt

# Tree view custom depth
eza --tree --icons --level=<depth>

# Fast file search by name (fd)
fd <pattern>

# Find files by name across all drives (plocate)
plocate <filename>

# Recursive text search (ripgrep)
rg '<pattern>'

# Ripgrep in specific file type
rg '<pattern>' -t <filetype>

# AST code-structure-aware search
sg '<pattern>' --lang <language>

# Interactive fuzzy file finder (fzf)
fzf

# SQL-like file queries (fselect)
fselect name from <dir> where size > 1mb

# Safely move file to trash
tp <file>

# List all items in trash
tl

# Interactively restore from trash
trs

# Empty trash permanently
te

# Decompress any archive
unpack <archive>

# Compress files into archive
pack <files...> <output.tar.gz>

# Copy file with progress bar
rsync -ah --progress <src> <dest>

# Sync two directories
rsync -avz --delete <src>/ <dest>/

% files, disk, backup, rclone

# List rclone remotes
rclone listremotes

# Copy local to remote
rclone copy <local_path> <remote>:<path>

# Sync local to remote (one-way)
rclone sync <local> <remote>:<path>

# Mount remote as filesystem
rclone mount <remote>:<path> <mountpoint> --daemon

# Show rclone remote stats
rclone about <remote>:

# Timeshift — create manual snapshot
sudo timeshift --create --comments "<label>"

# Timeshift — list snapshots
sudo timeshift --list

# Timeshift — restore snapshot
sudo timeshift --restore

% text, editor, markdown

# Open file in Neovim
v <file>

# Open file in Nano
nano <file>

# Render Markdown beautifully in terminal
md <file.md>

# View file with syntax highlighting (bat)
cat <file>

# View file with line numbers (bat)
bat -n <file>

# Replace text in file (modern sed)
sd '<old>' '<new>' <file>

# Replace across multiple files
sd '<old>' '<new>' $(fd -e <ext>)

# Select columns like cut/awk
choose <col_indices>

# Convert document to PDF (pandoc)
pandoc <input.md> -o <output.pdf>

# Convert Markdown to HTML
pandoc <input.md> -o <output.html>

% data, spreadsheets, json, csv

# Open CSV/JSON/XLSX in VisiData
vd <file>

# Query JSON/YAML/TOML values (dasel)
dasel -f <file> '<selector>'

# Pretty-print JSON (jq)
jq '.' <file.json>

# Flatten nested JSON to dotted paths (gron)
gron <file.json>

# Filter CSV rows (miller)
mlr --csv filter '$col == "<value>"' <file.csv>

# Stats on CSV column
mlr --csv stats1 -a mean,min,max -f <column> <file.csv>

# Convert CSV to JSON
mlr --csv --ojson cat <file.csv>

# CSV stats overview (xsv)
xsv stats <file.csv>

# Select CSV columns
xsv select <col1,col2> <file.csv>

% git, version control

# Open Lazygit TUI
lg

# Open GitUI TUI
gg

# Open GitHub TUI dashboard
ghd

# Clone repository
git clone <url>

# Stage all changes
git add -A

# Commit with message
git commit -m "<message>"

# Push current branch
git push

# Pull with rebase
git pull --rebase

# Pull, rebase, push (one command)
git sync

# Undo last commit, keep changes
git undo

# Add staged to last commit
git amend

# Visual branch graph
git graph

# View semantic diffs (difftastic)
dlog

# Stash changes
git stash

# Pop stash
git stash pop

# Create and switch to new branch
git checkout -b <branch>

# Delete local branch
git branch -d <branch>

# Interactive rebase last N commits
git rebase -i HEAD~<n>

# Cherry-pick a commit
git cherry-pick <hash>

# Show diff of staged changes
git diff --staged

# Search commits by message
git log --grep '<term>'

% github, gh cli

# Create a pull request
gh pr create

# List open PRs
gh pr list

# View PR status and checks
gh pr status

# Merge PR
gh pr merge <number>

# Create an issue
gh issue create

# List issues
gh issue list

# View CI/CD runs
gh run list

# Watch a workflow run live
gh run watch

# Clone a repo
gh repo clone <owner>/<repo>

# Fork a repo
gh repo fork <owner>/<repo>

% docker, containers

# Open Lazydocker TUI
ld

# Open Portainer Web GUI (localhost:9000)
dgui

# Live container metrics (ctop)
dtop

# Show running containers formatted
dps

# Show all containers including stopped
docker ps -a

# Start a container
docker start <name>

# Stop a container
docker stop <name>

# Exec shell into running container
docker exec -it <name> bash

# View container logs live
docker logs -f <name>

# Build image from Dockerfile
docker build -t <name>:<tag> .

# Pull image from registry
docker pull <image>:<tag>

# Prune stopped containers and dangling images
docker system prune -f

# Remove unused volumes
docker volume prune

# List all images
docker images

# Docker Compose — start services
docker compose up -d

# Docker Compose — stop services
docker compose down

# Docker Compose — logs
docker compose logs -f

# Docker Compose — rebuild and restart
docker compose up -d --build

% network, tunnels, scanning

# Expose local port via Cloudflare tunnel
share <port>

# Expose Vaultwarden port 8222
tunnel 8222

# Interactive LAN network scanner
netscanner

# Port scanner (nmap)
nmap -sV <host>

# Scan all ports
nmap -p- <host>

# Interactive traceroute with packet loss (trippy)
trip <host>

# DNS lookup (dog — modern dig)
dog <domain>

# DNS lookup specific record type
dog <domain> <type>

# Visual real-time ping graph
gping <host1> <host2>

# Real-time bandwidth per process
bandwhich

# Show open ports and connections
ss -tulpn

# Send GET request with colored output
xh get <url>

# Send POST with JSON body
xh post <url> key=value

# Download file
wget <url>

# Download file multi-connection (aria2c)
aria2c -x 16 <url>

# ARP scan LAN
arp-scan -l

% security, vpn, passwords

# Unlock Bitwarden vault (persists session)
bw-unlock

# Open self-hosted Vaultwarden Web Vault
vw

# Point BW CLI to local Vaultwarden
bw-local

# Generate secure 24-char password
genpass

# Generate 4-word passphrase
genphrase

# List Bitwarden items
bw list items

# Get password for item
bw get password <item_name>

# Search Bitwarden items
bw list items --search <query>

# rbw list (no master password prompt)
rbw list

# rbw get password
rbw get <item>

# ProtonVPN — connect fastest server
protonvpn-cli connect --fastest

# ProtonVPN — connect to country
protonvpn-cli connect --cc <CC>

# ProtonVPN — disconnect
protonvpn-cli disconnect

# ProtonVPN — status
protonvpn-cli status

# SSH to remote host
ssh <user>@<host>

# Copy SSH key to remote
ssh-copy-id <user>@<host>

% media, video, audio, download

# Open NCMPCPP music player
music

# Open CAVA audio visualizer
vis

# Play/pause media (playerctl)
playerctl play-pause

# Next track
playerctl next

# Previous track
playerctl previous

# Volume up (pamixer)
pamixer -i 5

# Volume down
pamixer -d 5

# Mute/unmute toggle
pamixer -t

# Download video best quality
ytdl-best <url>

# Download audio as MP3
ytdl-mp3 <url>

# Download video (yt-dlp)
ytdl <url>

# Download playlist
yt-dlp -o '%(playlist_index)s-%(title)s.%(ext)s' <playlist_url>

# Download Spotify track/album/playlist
spot <url>

# Download images from gallery sites
gdl <url>

# Play video in terminal (mpv)
mpv <file>

# Play video with subtitles
mpv --sub-file=<subtitle.srt> <video>

# Convert video format (ffmpeg)
ffmpeg -i <input> <output.mp4>

# Extract audio from video
ffmpeg -i <input.mp4> -q:a 0 -map a <output.mp3>

# Record screen (ffmpeg)
ffmpeg -f x11grab -r 30 -s 1920x1080 -i :0.0 <output.mp4>

# Compress video (HandBrake CLI)
HandBrakeCLI -i <input> -o <output.mp4> --preset "Fast 1080p30"

# Show media file info (mediainfo)
mediainfo <file>

# Display image in terminal (chafa)
chafa <image>

# Display image in terminal (timg)
timg <image>

# Open image viewer (imv)
imv <image>

# Convert image format (imagemagick)
convert <input.png> <output.jpg>

# Resize image
convert <input> -resize 1920x1080 <output>

# Image to ASCII art (jp2a)
jp2a <image>

% recording, screenshots

# Screenshot with selection (flameshot)
flameshot gui

# Screenshot full screen to file
maim ~/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png

# Screenshot selection to clipboard
maim -s | xclip -selection clipboard -t image/png

# Record terminal session (asciinema)
asciinema rec <output.cast>

# Play recorded terminal session
asciinema play <file.cast>

# Upload recording to asciinema.org
asciinema upload <file.cast>

# Open OBS Studio
obs

% development, coding, tools

# Count lines of code by language (tokei)
tokei

# Count and sort by lines
tokei --sort code

# Generate regex from examples (grex)
grex <example1> <example2>

# Benchmark command speed (hyperfine)
bench '<command>'

# Compare two benchmarks
hyperfine '<cmd1>' '<cmd2>'

# Format/query JSON (jq)
jq '<filter>' <file.json>

# HTTP API client (xh)
xh get <url>

# AI pair programming (aider)
aider

# Aider with specific model
aider --model <model>

# Open Postman
postman

# Maven build
mvn clean install

# Maven run Spring Boot
mvn spring-boot:run

# Gradle build
gradle build

# Gradle run
gradle bootRun

# pnpm install
pnpm install

# pnpm dev server
pnpm dev

# pnpm build
pnpm build

# Install runtime version (mise)
mise install <runtime>@<version>

# Use runtime version in project
mise use <runtime>@<version>

# Allow .envrc (direnv)
direnv allow

# Spell-correct last command (thefuck)
fuck

% ai, antigravity

# Start Antigravity interactive session
agy

# Resume last Antigravity conversation
agy-last

# Quick one-off AI question
agy-ask "<prompt>"

# AI pair programming with Aider
aider

% databases

# Connect to PostgreSQL
psql -U <user> -d <database>

# Connect to PostgreSQL on host
psql -h <host> -U <user> -d <database>

# Dump PostgreSQL database
pg_dump -U <user> <database> > backup.sql

# Restore PostgreSQL database
psql -U <user> <database> < backup.sql

# Start PostgreSQL service
sudo systemctl start postgresql

# Connect to MariaDB/MySQL
mysql -u <user> -p <database>

# Open DBeaver GUI database client
dbeaver

% multiplexer, sessions

# Attach to main tmux session or create new
tma

# List tmux sessions
tml

# Kill tmux session
tmk <session_name>

# Open Zellij multiplexer
zellij

# Zellij — attach to session
zellij attach <session>

# Zellij — list sessions
zellij list-sessions

% tasks, time, calendar

# Add high-priority task with deadline
t add "<task>" priority:H due:tomorrow

# List pending tasks
t

# Complete a task
t <id> done

# Delete a task
t <id> delete

# Filter tasks by project
t project:<name>

# Start time tracking (timew)
tw start "<task>"

# Stop time tracking
tw stop

# Time summary today/this week
tw summary

# View upcoming calendar events (khal)
agenda

# Open khal interactive TUI
kc

# Open calcurse TUI calendar
cal

# Add event to khal
khal new <datetime> <duration> '<title>'

% android, sync, remote

# Screen mirror Android device (scrcpy)
scrcpy

# ADB shell into device
adb shell

# ADB list connected devices
adb devices

# ADB push file to device
adb push <local> <remote>

# ADB pull file from device
adb pull <remote> <local>

# ADB install APK
adb install <file.apk>

# KDE Connect — list paired devices
kdeconnect-cli -l

# KDE Connect — send file to phone
kdeconnect-cli --share <file> -n <device>

# Open LocalSend file sharing app
localsend

# Open Syncthing Web UI
syncthing

% clipboard

# Copy text to clipboard
echo "<text>" | copy

# Paste from clipboard
paste

# Copy file contents to clipboard
cat <file> | copy

# Open CopyQ clipboard manager
copyq show

% translation

# Translate to English
tren "<text>"

# Translate to French
trfr "<text>"

# Translate to Arabic
trar "<text>"

# Translate between languages
tr <src>:<target> "<text>"

% hdd, storage routing

# View HDD routing status
hdd-route status

# Route Steam to HDD
hdd-route steam

# Route VSCode to HDD
hdd-route vscode

# Route Docker to HDD
hdd-route docker

# Route Flatpak to HDD
hdd-route flatpak

% i3, desktop, window manager

# Reload i3 config
i3-msg reload

# Restart i3 in-place
i3-msg restart

# Open rofi launcher
rofi -show drun

# Open rofi window switcher
rofi -show window

# Open rofi calculator
rofi -show calc

# Open Thunar file manager
thunar

% pdf, documents

# Generate TOC from PDF (pdftocgen)
pdftocgen <file.pdf>

# Import TOC into PDF (pdftocio)
pdftocio <file.pdf>

# Extract PDF metadata (pdfxmeta)
pdfxmeta <file.pdf>

# Convert PDF pages to images
convert -density 300 <file.pdf> page-%03d.png

# Compress PDF (ghostscript)
gs -sDEVICE=pdfwrite -dPDFSETTINGS=/ebook -o <out.pdf> <in.pdf>

% fun, eye candy

# Animated bonsai tree
bonsai

# ASCII fish tank
aquarium

# Matrix digital rain
matrix

# Pipes screensaver
pipes.sh

# ASCII art banner
banner "<text>"

# Colored styled banner (toilet)
cbanner "<text>"

# Rainbow-colorize output
<command> | rainbow

# Draw a box around text
echo "<text>" | box

# System info with ASCII art (fastfetch)
fastfetch

# Terminal Wordle game
wordle

% dev, mise, toolchains

# List all installed toolchains and versions (mise)
mise ls

# Install and globally activate a runtime tool (e.g. bun, zig, rust)
mise use -g <tool>@latest

# Upgrade all mise managed toolchains
mise upgrade

# Check mise health and active paths
mise doctor

% dev, zig

# Initialize a new Zig project
zig init

# Build and run a Zig project
zig build run

# Run a single Zig source file directly
zig run <file.zig>

# Compile a standalone optimized release binary
zig build-exe -O ReleaseFast <file.zig>

# Run Zig tests
zig test <file.zig>

% dev, bun, javascript, typescript

# Initialize a new Bun project
bun init

# Run a script or TS/JS file with Bun
bun run <file.ts>

# Install all package dependencies with Bun
bun install

# Add a package to dependencies
bun add <package>

# Run Bun unit tests
bun test

# Bundle application for production
bun build <entrypoint.ts> --outdir <./dist>

% dev, rust, cargo

# Create a new binary Rust project
cargo new <project_name>

# Build and run the project
cargo run

# Build optimized release binary
cargo build --release

# Run Rust project tests
cargo test

# Fast syntax and type checking without producing a binary
cargo check

# Add a dependency crate to Cargo.toml
cargo add <crate_name>

% dev, golang, go

# Initialize a new Go module
go mod init <module_path>

# Run a Go file directly
go run <file.go>

# Build a Go binary
go build -o <binary_name>

# Run Go tests with coverage
go test -v -cover ./...

# Download and add a Go dependency
go get <package_path>

# Clean unused dependencies in go.mod / go.sum
go mod tidy

% dev, dotnet, csharp

# Create a new console project (.NET)
dotnet new console -o <project_name>

# Create a new ASP.NET Core Web API project
dotnet new webapi -o <project_name>

# Build and run the current .NET project
dotnet run

# Build the .NET project
dotnet build -c Release

# Run .NET tests
dotnet test

# Add a NuGet package to project
dotnet add package <package_name>

% dev, gleam

# Create a new Gleam project
gleam new <project_name>

# Run the Gleam project
gleam run

# Run Gleam tests
gleam test

# Add a Gleam dependency
gleam add <package_name>

# Format all Gleam code
gleam format

% dev, nim

# Compile and run a Nim file immediately
nim c -r <file.nim>

# Compile an optimized release binary
nim c -d:release --opt:speed <file.nim>

# Initialize a new Nimble project
nimble init

# Build and run with Nimble package manager
nimble build

% dev, vlang, v

# Initialize a new V project
v init

# Run a V source file
v run <file.v>

# Build an optimized production binary
v -prod -o <binary_name> <file.v>

# Format V code
v fmt -w <file.v>

% dev, crystal

# Create a new Crystal application
crystal init app <app_name>

# Run a Crystal file directly
crystal run <file.cr>

# Build an optimized production binary
crystal build --release <file.cr>

# Run Crystal spec tests
crystal spec

% dev, elixir, erlang

# Create a new Mix Elixir project
mix new <project_name>

# Run the project main entrypoint
mix run

# Start interactive Elixir shell with project context
iex -S mix

# Fetch and install dependencies
mix deps.get

# Run Mix tests
mix test

% dev, haskell, cabal, ghc

# Compile a Haskell file to native binary
ghc <file.hs> -o <binary_name>

# Start interactive GHCi REPL
ghci

# Initialize a new Cabal project
cabal init --interactive

# Build and run with Cabal
cabal run

# Update Cabal package list
cabal update

% dev, julia

# Execute a Julia expression from command line
julia -e '<code_snippet>'

# Start Julia REPL with current directory environment
julia --project

# Run a Julia script
julia <script.jl>

% devops, terraform, cloud

# Initialize Terraform workspace and providers
terraform init

# Show proposed infrastructure changes
terraform plan

# Apply infrastructure changes
terraform apply -auto-approve

# Destroy managed infrastructure
terraform destroy

# Format all Terraform configuration files
terraform fmt -recursive

% devops, helm, kubernetes

# Add a Helm chart repository
helm repo add <repo_name> <repo_url>

# Update Helm repositories
helm repo update

# Install a Helm release into Kubernetes cluster
helm install <release_name> <repo_name>/<chart_name>

# List all Helm releases across all namespaces
helm list -A

# Upgrade an existing Helm release
helm upgrade <release_name> <repo_name>/<chart_name>

% documents, typst, quarto

# Initialize a new Typst document
typst init <template> <project_name>

# Compile a Typst document to PDF
typst compile <document.typ> <output.pdf>

# Watch Typst file and auto-recompile on changes
typst watch <document.typ>

# Render a Quarto document to HTML/PDF
quarto render <document.qmd>

# Live preview a Quarto document with hot-reloading
quarto preview <document.qmd>
